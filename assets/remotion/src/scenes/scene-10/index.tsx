import { Url } from "./url";

// Scene 10 — URL. 120 frames @ 60fps (2s). Global 1890–2010.
// Beats (local frames):
//   0–12     URL plain opacity fade in.
//   12–102   Hold.
//   102–120  Dissolve to white (Scene's default 18-frame exit).

export const Scene10: React.FC = () => <Url />;
