import { Heading } from "./heading";

// Scene 9 — Research initiative. 120 frames @ 60fps (2s). Global 1770–1890.
// Beats (local frames):
//   0–24     Heading letter-spacing collapse.
//   24–102   Hold.
//   102–120  Dissolve to white (Scene's default 18-frame exit).

export const Scene9: React.FC = () => <Heading />;
