bronze.ctd.ctd_exposure_events:
  type: polars.CSVDataset
  filepath: "data/bronze/ctd/ctd_exposure_events.csv"
  load_args:
    infer_schema: false
    schema:
      "exposure_stressor_name": "${pl:Utf8}"
      "exposure_stressor_id": "${pl:Utf8}"
      "stressor_source_category": "${pl:Utf8}"
      "stressor_source_details": "${pl:Utf8}"
      "number_of_stressor_samples": "${pl:Utf8}"
      "stressor_notes": "${pl:Utf8}"
      "number_of_receptors": "${pl:Utf8}"
      "receptors": "${pl:Utf8}"
      "receptor_notes": "${pl:Utf8}"
      "smoking_status": "${pl:Utf8}"
      "age": "${pl:Utf8}"
      "age_units_of_measurement": "${pl:Utf8}"
      "age_qualifier": "${pl:Utf8}"
      "sex": "${pl:Utf8}"
      "race": "${pl:Utf8}"
      "methods": "${pl:Utf8}"
      "detection_limit": "${pl:Utf8}"
      "detection_limit_uom": "${pl:Utf8}"
      "detection_frequency": "${pl:Utf8}"
      "medium": "${pl:Utf8}"
      "exposure_marker": "${pl:Utf8}"
      "exposure_marker_id": "${pl:Utf8}"
      "marker_level": "${pl:Utf8}"
      "marker_units_of_measurement": "${pl:Utf8}"
      "marker_measurement_statistic": "${pl:Utf8}"
      "assay_notes": "${pl:Utf8}"
      "study_countries": "${pl:Utf8}"
      "state_or_province": "${pl:Utf8}"
      "city_town_region_area": "${pl:Utf8}"
      "exposure_event_notes": "${pl:Utf8}"
      "outcome_relationship": "${pl:Utf8}"
      "disease_name": "${pl:Utf8}"
      "disease_id": "${pl:Utf8}"
      "phenotype_name": "${pl:Utf8}"
      "phenotype_id": "${pl:Utf8}"
      "phenotype_action_degree_type": "${pl:Utf8}"
      "anatomy": "${pl:Utf8}"
      "exposure_outcome_notes": "${pl:Utf8}"
      "reference": "${pl:Utf8}"
      "associated_study_titles": "${pl:Utf8}"
      "enrollment_start_year": "${pl:Utf8}"
      "enrollment_end_year": "${pl:Utf8}"
      "study_factors": "${pl:Utf8}"
  metadata:
    checksum: "77b23041f8ea7fc4950c11381e8c3ec3"
    kedro-viz:
      layer: bronze
