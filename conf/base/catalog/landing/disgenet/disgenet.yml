landing.disgenet.curated_gene_disease_associations:
  type: polars.CSVDataset
  filepath: "data/landing/disgenet/curated_gene_disease_associations.tsv"
  load_args:
    separator: "\t"
    infer_schema: false
    schema:
      "geneid": "${pl:String}"
      "geneSymbol": "${pl:String}"
      "DSI": "${pl:Float64}"
      "DPI": "${pl:String}"
      "diseaseId": "${pl:String}"
      "diseaseName": "${pl:String}"
      "diseaseType": "${pl:String}"
      "diesaseClass": "${pl:String}"
      "diesaseSemanticType": "${pl:String}"
      "score": "${pl:Float64}"
      "EI": "${pl:Float64}"
      "YearInitial": "${pl:String}"
      "YearFinal": "${pl:String}"
      "NofPmids": "${pl:String}"
      "NofSnps": "${pl:String}"
      "source": "${pl:String}"
  metadata:
    checksum: 132b5f1cc77db4877c2be0b555c17e94
    kedro-viz:
      layer: landing