landing.bgee.homo_sapiens_expressions_advanced:
  type: polars.CSVDataset
  filepath: "data/landing/bgee/Homo_sapiens_expr_advanced.tsv.gz"
  load_args:
    separator: "\t"
    infer_schema: false
    null_values: ["NA"]
    schema:
      # Note: Updated schema based on newer Bgee format. Types inferred.
      "Gene ID": "${pl:String}"
      "Gene name": "${pl:String}"
      "Anatomical entity ID": "${pl:String}"
      "Anatomical entity name": "${pl:String}"
      "Expression": "${pl:String}"
      "Call quality": "${pl:String}"
      "FDR": "${pl:String}"
      "Expression score": "${pl:Float64}"
      "Expression rank": "${pl:Float64}"
      "Including observed data": "${pl:Utf8}"
      "Self observation count": "${pl:Int64}"
      "Descendant observation count": "${pl:Int64}"
      "Affymetrix expression": "${pl:String}"
      "Affymetrix call quality": "${pl:String}"
      "Affymetrix FDR": "${pl:String}"
      "Affymetrix expression score": "${pl:Float64}"
      "Affymetrix expression rank": "${pl:Float64}"
      "Affymetrix weight for expression rank and score": "${pl:Float64}"
      "Including Affymetrix observed data": "${pl:Utf8}"
      "Self observation count Affymetrix": "${pl:Int64}"
      "Descendant observation count Affymetrix": "${pl:Int64}"
      "EST expression": "${pl:String}"
      "EST call quality": "${pl:String}"
      "EST FDR": "${pl:String}"
      "EST expression score": "${pl:Float64}"
      "EST expression rank": "${pl:Float64}"
      "EST weight for expression rank and score": "${pl:Float64}"
      "Including EST observed data": "${pl:Utf8}"
      "Self observation count EST": "${pl:Int64}"
      "Descendant observation count EST": "${pl:Int64}"
      "in situ hybridization expression": "${pl:String}"
      "in situ hybridization call quality": "${pl:String}"
      "in situ hybridization FDR": "${pl:Float64}"
      "in situ hybridization expression score": "${pl:Float64}"
      "in situ hybridization expression rank": "${pl:Float64}"
      "in situ hybridization weight for expression rank and score": "${pl:Float64}"
      "Including in situ hybridization observed data": "${pl:Utf8}"
      "Self observation count in situ hybridization": "${pl:Int64}"
      "Descendant observation count in situ hybridization": "${pl:Int64}"
      "RNA-Seq expression": "${pl:String}"
      "RNA-Seq call quality": "${pl:String}"
      "RNA-Seq FDR": "${pl:Float64}"
      "RNA-Seq expression score": "${pl:Float64}"
      "RNA-Seq expression rank": "${pl:Float64}"
      "RNA-Seq weight for expression rank and score": "${pl:Float64}"
      "Including RNA-Seq observed data": "${pl:Utf8}"
      "Self observation count RNA-Seq": "${pl:Int64}"
      "Descendant observation count RNA-Seq": "${pl:Int64}"
      "single-cell RNA-Seq expression": "${pl:String}"
      "single-cell RNA-Seq call quality": "${pl:String}"
      "single-cell RNA-Seq FDR": "${pl:Float64}"
      "single-cell RNA-Seq expression score": "${pl:Float64}"
      "single-cell RNA-Seq expression rank": "${pl:Float64}"
      "single-cell RNA-Seq weight for expression rank and score": "${pl:Float64}"
      "Including single-cell RNA-Seq observed data": "${pl:Utf8}"
      "Self observation count single-cell RNA-Seq": "${pl:Int64}"
      "Descendant observation count single-cell RNA-Seq": "${pl:Int64}"
  metadata:
    checksum: "a845f239f13469219ba6debf574208aa"
    origin:
      provider: http
      url: "https://www.bgee.org/ftp/current/download/calls/expr_calls/Homo_sapiens_expr_advanced.tsv.gz" # NOTE: polars can handle gzip files
